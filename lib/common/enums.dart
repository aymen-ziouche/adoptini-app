

enum PetGender{
  male,
  female,
}
enum PetSize{
  big,
  medium,
  small,
}

enum PetType{
  all,
  cat,
  dog,
  bird,
  other,
}

enum TicketType {
  bug,
  feature,
  suggestion,
  help,
}

enum Languages { english, french, arabic }
